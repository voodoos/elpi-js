(jbuild_version 1)

(executable
 ((name main)
  (libraries (js_of_ocaml js_of_ocaml.weak elpi base64))
  (js_of_ocaml ()))) ;(flags (:standard --file "files"))

(copy_files data/*)

(alias
 ((name DEFAULT)
  ; Would be nice to have data packing at compile time...
  ;(action (with-stdout-to "data.ml" (run python pack_data.py)))
  (deps (main.bc.js)))) ;(glob_files data/*)
